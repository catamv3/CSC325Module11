package org.example.NewDrawingSys.oldDrawingSys;

/**
 *
 * @author MoaathAlrajab
 */
public class Circle {
    public void draw(int x1, int y1, int r) {
        System.out.println("Circle from center (" + x1 + ";" + y1 + "), with radius(" + r + ")");
    }
}